import { education } from '../constants';

const Education = ({ school, earned, date }) => {
    return (
        <>
            <div>
                <p className="text-2xl">{education}</p>
            </div>
        </>
    );
};

export default Education;
